SECRET_KEY=django-insecure-replace-this-with-a-real-secret-key

# Default runtime mode (mock providers + test OTP when true). This is only the
# fallback default — flip it live without redeploying from Django admin:
# /admin/ -> Admin_Portal -> Runtime configuration.
DEV_MODE=true
TEST_OTP=123456

# PostgreSQL (prod only)
PG_DB=topupnaija
PG_USER=postgres
PG_PASSWORD=
PG_HOST=localhost
PG_PORT=5432

# Allowed hosts (prod)
ALLOWED_HOSTS=topupnaija.com,api.topupnaija.com

# SMS — Africa's Talking
AT_API_KEY=
AT_USERNAME=your_at_username
AT_SENDER_ID=TopUpNaija

# Gladtiding
GLADTIDING_API_KEY=
GLADTIDING_BASE_URL=https://gladtidingsdata.com/api
